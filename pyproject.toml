[project]
name = "visualize-csv-webapp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0",
    "flask-socketio>=5.3",
    "pandas>=2.0",
    "numpy>=1.24",
    "requests>=2.31"
]

[dependency-groups]
dev = []

[tool.uv]
